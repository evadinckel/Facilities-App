const assert = require('assert');
const Vote = require('../../database/vote.js');

// describe('Votes', () => {
//   it('#can create votes', () => {
//     var vote = new Vote({ voteValue: 1 });
//     vote.save();
//
//     Vote.find({ _id: vote._id.toString() }).then(data => {
//       console.log(data._id);
//     });
//     // Votes.find({ _id: vote._id}).then(data) => {
//     //
//     //   assert(data[0].value === 1);
//     // });
//   });
// });
